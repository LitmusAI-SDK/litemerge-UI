# litemerge-UI

Frontend dashboard for LitmusAI behavioral testing.

## Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
npm run preview
```

## Environment

- `VITE_API_URL` (optional): backend base URL.
- Defaults to `http://localhost:8000` if unset.
